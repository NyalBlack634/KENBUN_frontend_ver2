/** 一度に取り出す見出しの上限 */
var limit_num = 20;

var talkAddOption = {
    position: {
        /** フレームの表示位置 */
        frameTop: `${zoomViewPort.top + window.pageXOffset}px`,
    },
    animation: {
        /** アニメーションの向きを制御する係数 */
        vector: 1
    }
}
